import { useMemo, useState } from "react";
import { CheckCircle2, Clock3, Tag, Warehouse } from "lucide-react";
import PageHeader from "../../../components/layout/PageHeader.jsx"; import Card from "../../../components/ui/Card.jsx"; import MetricCard from "../../../components/ui/MetricCard.jsx"; import FactoryPagination, { useFactoryClientPagination } from "../components/FactoryPagination.jsx"; import { FactoryTable } from "../components/FactoryDataDisplay.jsx"; import useFactoryMasterData from "../hooks/useFactoryMasterData.js"; import useFactoryNavigation from "../hooks/useFactoryNavigation.js"; import useFactoryPermissions from "../hooks/useFactoryPermissions.js";
export default function FactoryStorageLocationsPage(){const {storageLocations}=useFactoryMasterData();const {can}=useFactoryPermissions();const nav=useFactoryNavigation();const [search,setSearch]=useState("");const rows=useMemo(()=>storageLocations.filter(x=>`${x.location_name||""} ${x.location_code||""} ${x.location_type||""}`.toLowerCase().includes(search.toLowerCase())),[storageLocations,search]);const pager=useFactoryClientPagination("storage-locations",rows.length,20,search);const active=storageLocations.filter(x=>x.status==="active");return <div className="space-y-5"><PageHeader section="System" title="Storage Locations" description="Manage Factory warehouse and production storage locations used by raw material and finished goods master records." actions={can("factory_storage_locations.create")||can("factory_storage_locations.manage")?<button className="btn-primary" type="button" onClick={nav.openCreateStorageLocation}><Warehouse size={15}/> Storage Location</button>:null}/><div className="grid gap-3 md:grid-cols-4"><MetricCard icon={Warehouse} label="Total Locations" value={storageLocations.length} helper="Active and archived"/><MetricCard icon={CheckCircle2} label="Active" value={active.length} helper="Available for selection" tone="success"/><MetricCard icon={Clock3} label="Archived" value={storageLocations.length-active.length} helper="Historical locations"/><MetricCard icon={Tag} label="Location Types" value={new Set(active.map(x=>x.location_type).filter(Boolean)).size} helper="Active type coverage"/></div><Card title="Storage Location Master"><input className="field-input mb-4" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search storage locations"/><FactoryTable rows={rows.slice(pager.from,pager.to)} columns={[{key:"location",label:"Location",render:r=><div><div className="font-bold">{r.location_name||"—"}</div><div className="text-xs text-text-secondary">{r.location_code||"—"}</div></div>},{key:"type",label:"Type",render:r=>r.location_type||"—"},{key:"status",label:"Status",render:r=>r.status||"—"},{key:"actions",label:"Actions",align:"right",render:r=><div className="flex justify-end gap-2">{(can("factory_storage_locations.edit")||can("factory_storage_locations.manage"))?<button className="btn-secondary text-xs" type="button" onClick={()=>nav.openEditStorageLocation(r)}>Edit</button>:null}{(can("factory_storage_locations.delete")||can("factory_storage_locations.manage"))&&r.status!=="archived"?<button className="btn-secondary text-xs" type="button" onClick={()=>nav.archiveStorageLocation(r)}>Archive</button>:null}</div>}]} emptyTitle="No storage locations"/><FactoryPagination page={pager.page} pageSize={pager.pageSize} total={rows.length} onPageChange={pager.setPage} onPageSizeChange={pager.setPageSize}/></Card></div>}
