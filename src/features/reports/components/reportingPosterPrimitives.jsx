import "./reportingPoster.css";
import "./reportingPosterOverrides.css";

export function PosterShell({ kind, children, label }) { return <article className={`report-poster report-poster--${kind}`} aria-label={label}><div className="report-poster__inside">{children}</div></article>; }
export function PosterHeader({ type, outlet, period, status, incomplete, logoUrl }) { const name = outlet?.name ?? "Outlet"; return <header className="report-poster__head"><div className="report-poster__identity">{logoUrl ? <img className="report-poster__logo" src={logoUrl} alt={`${name} logo`} /> : <span className="report-poster__wordmark">{name}</span>}</div><div className="report-poster__heading"><div><div className="report-poster__type">{type} <span>· {period}</span></div><h2 className="report-poster__title">{name}</h2></div><span className={`report-poster__status ${incomplete ? "is-incomplete" : ""}`}>{status}</span></div></header>; }
export function PosterFooter({ type, period, status }) { return <footer className="report-poster__footer"><span>{type}</span><span>{period}</span><span>Generated report · {status}</span></footer>; }
export function MetricValue({ label, metric, percentage, qualifier, className = "" }) { return <div className={`poster-metric ${className}`}><span className="poster-metric-label">{label}</span>{qualifier ? <span className="poster-metric-qualifier">{qualifier}</span> : null}<strong className="poster-value">{metric}</strong><span className="poster-metric-percent">{percentage}</span></div>; }
